# Eng1300 开发规范

自考英语（二）/英语（专升本）真题练习系统。Cloudflare Workers + D1 + Hono 后端，
React 18 + Vite 前端，部署在 workers.dev，公开免费给学员用。

文档分三层，不要在一层里写另一层的内容：

| 文件 | 内容 | 什么时候看 |
|---|---|---|
| `docs/开发标准.md` | 与项目无关的通用要求，可整份复制到别的项目 | 写代码和写测试之前 |
| 本文件 | 本项目的环境限制、命令、以及标准在本项目的具体落实 | 每次开工 |
| `docs/开发踩坑记录.md` | 触发这些规则的真实案例与根因 | 遇到相似症状时对照 |

**内容全部来自真实踩过的坑，不是通用最佳实践的摘抄。**

---

## 一、环境限制（先读这段，能省一小时）

**出站被拦。** 沙箱的出站策略拒绝 `workers.dev`、`api.cloudflare.com`、
`api.siliconflow.cn`。所以：

- 部署、线上验证、真实 AI 调用，一律走 GitHub Actions，本机 curl 不到线上。
- 不要试图绕过代理，也不要关掉 TLS 校验。碰到 403 就照实说。
- 只能看流水线日志来判断线上状态。

**`wrangler dev` 启动要 100 秒以上**，因为它启动时要连几个被拒的
cloudflare.com 地址，得重试到超时。测试脚本的就绪等待是 150 秒，等不到会带
dev 日志尾部报错退出。不要把这个等待改短。

**杀服务要杀整个进程组。** `wrangler dev` 会派生 workerd 子进程真正监听端口，
只杀 wrangler 本身杀不掉它，它会继续占着端口，下一轮起不来（报 Address already
in use，同时提示一个已删除的构建临时路径，很容易把注意力引到构建上去）。
用 `setsid` 起、`kill -- -$PGID` 收。

**不要用 `pgrep -f` / `pkill -f` 找 wrangler**，模式会匹配到你自己这条命令，
把自己的 shell 杀掉。用 `ps -eo pid,args | awk '/wrangler[ ]dev/ {print $1}'`。

**每套测试占独立端口**，加新测试前先查这张表：

| 套件 | 服务端口 | AI 替身端口 |
|---|---|---|
| m2-smoke | 8791 | — |
| m3-smoke | 8792 | — |
| m4-smoke | 8793 | — |
| m5-smoke | 8794 | 8899 |
| m6-acceptance | 8796 | 8898 |
| ui-smoke | 8798 | — |

**LibreOffice 不可用**（连最小 docx 都报 source file could not be loaded），
生成 Word 后没法转 PDF 看版式。只能做 schema 校验加读回正文核对，版式要如实
告诉用户你没亲眼确认过。

**不要用 `sleep` 链式等待**，用 Bash 工具的 `run_in_background`。也不要在
bash 正在执行某个脚本时去编辑它——会在毫不相干的行报语法错误。

---

## 二、常用命令

```bash
# 全套回归（推送前必跑）
cd worker && for s in m2-smoke m3-smoke m4-smoke m5-smoke m6-acceptance; do
  echo "=== $s ==="; bash test/$s.sh 2>&1 | grep -E "FAIL|小结" || echo "  !! 没有小结"
done
node test/quota-degrade.mjs && node test/essay-parse.mjs

# 浏览器实测（手机/平板/PC 三种宽度）
cd worker && bash test/ui-smoke.sh

# 重新生成题库种子
node scripts/build-seed-sql.mjs

# 构建前端（产物进 worker/public，由 Worker 静态托管）
cd web && npx vite build

# 生成学员手册
node scripts/build-user-manual.js
```

`|| echo "!! 没有小结"` 这一句不能省：脚本可能在服务起不来时提前退出，没有它
你会以为"没有 FAIL 就是通过了"。

---

## 三、铁律

完整规则在 `docs/开发标准.md`（29 条，与项目无关）。但那份不会自动进上下文，所以
把最容易在赶工时忘掉的几条按"动作"抄在下面。**动手写测试、接外部依赖、或准备提交
之前，先扫一眼这一节；需要展开的理由和检验方法再去翻标准。**

**写测试时**

- 每条断言写完，把被测逻辑临时改成返回常量跑一次，确认变红；红不了这条就是装饰。
- 期望值和被测值不能同源（用库里的 X 去对接口返回的 X＝恒等式）。
- 会随业务规则变的数字不许写死，从数据现算。
- 挑样本前先确认它在被测流程里真的可达，并把决定可达性的那个属性打印出来。
  **按 id、字母序、插入顺序取"第一条"是这个错误的高发写法。**
- 只断言被测逻辑承诺过的性质，不要把自己的直觉当规格。

**接外部依赖时**

- 替身只能证明协议约束和降级路径，**不能证明"功能可用"**。时延和返回结构是它的
  结构性盲区——两者出事时都不报错，表现为"成功但结果不对"。
- 发布前对真实服务跑一次端到端。
- 对同一个慢依赖的 N 次调用写成有上限的并发，不要串行循环。判据：单次耗时 ×
  最大次数 超过 30 秒就必须改。

**处理错误时**

- 读不到值就抛错，不要回落默认值——默认值（0 / 空 / false）往往恰好是合法结果，
  于是"我没读懂"被伪装成"结果就是这样"。
- 核心计算失败要抛错；记账类写入失败要吞掉并告警。判据：**这次失败改变了用户要的
  那个结果吗**。
- 解析类错误信息里带上实际收到的内容特征（顶层键名、类型、前若干字符）。

**改代码时**

- 文本锚点取完整语法单元（整行），不要取会出现在更大结构中间的片段。
- 静态检查（`node --check`、`bash -n`）通过之后，必须真跑一次。
- 提交前跑全套，不只跑改动那套；汇总输出要能区分"通过"和"没跑到结论"。

下面是上述规则在本项目的具体落实，带具体参数：

### 断言的观测对象（标准 §1.4）

专项练习按"历史做过次数"升序出题。所以验证订正规则时，要挑**所属考点题目最少**
的那道错题，否则有限轮次根本轮不到它，测出来的是题库深度。验证权重时反过来，
要取**题量最多**的考点，否则题不够抽，权重再高也命中不了几次。

一道题可以同时挂多个考点。摸底阶段保证的是"每道题都带来至少一个新考点"，不是
"考点不重复"（标准 §1.5）。

### 替身的盲区（标准 §2.1、§2.3）

本地 AI 替身是 `worker/test/ai-stub.mjs`，它按我们要的形状瞬间返回。真实服务商
（硅基流动 Qwen3-8B）一次调用 3-4 秒。**上线前必须手动触发一次 `prod-e2e` 流水线**
（会真的花钱调 AI）。错题分析的并发上限见 `WRONG_ANALYZE_CONCURRENCY`。

Qwen3-8B 是推理模型，所有结构化调用必须带 `enable_thinking: false`，否则约三分之
二的请求返回空对象、延迟从 3 秒涨到 15 秒以上（实测结论，见 `docs/prd.md` §10.0）。

### D1 免费额度（标准 §5）

每天 10 万行写入，世界时零点（北京时间早八点）重置。耗光过一次，整站不可用。

- 种子文件的内容指纹由 `build-seed-sql.mjs` 写成该文件的最后一条语句，与数据同属
  一次 `d1 execute --file` 导入。
- 流水线顺序：迁移 → 部署 → 写密钥 → 导题库。
- 排查看 `wrangler d1 execute` 每次打印的 `rows_read` / `rows_written`。

### 提交前

跑全套回归（见上一节命令）。`|| echo "!! 没有小结"` 那句不能省——脚本可能在服务
起不来时提前退出（标准 §4.4）。

---

## 四、部署与线上验证

推送到 `claude/english-exam-question-bank-a8nbiu` 会自动触发
`.github/workflows/deploy-worker.yml`。顺序是：迁移 → 部署 → 写密钥 → 导题库 →
放行 → 初始化账号 → 线上验证。

线上验证里有一条"写入已恢复（AI 配置 N 秒前刚落库）"是写入哨兵。**额度用尽时
读接口全都正常，只有写会失败**，所以其余十几条读类断言全绿也说明不了站点能用。
登录以前能当哨兵（要写最后登录时间），改成额度用尽也放行之后就不能了。

`prod-e2e.yml` 是线上端到端实测，只手动触发，会真的调 AI 花钱、占写入额度。
它用探针账号 `PROBE01`（每次跑之前重置密码取随机口令，不必存任何 Secret，也
不污染 T001–T010 真学员的记录）。

**只手动触发的流水线在非默认分支上注册不了**：GitHub 要它至少跑过一次才认得，
而没注册就没法手动触发（dispatch 返回 404）。给一个 paths 限定到它自己文件的
push 触发来破这个死结。

**流水线日志有长度上限。** 大段内联 shell 会把日志尾部撑满，真正的错误被挤掉。
超过几行的逻辑放进 `scripts/ci/*.sh`。

---

## 五、其他约定

- 仓库是**公开**的。线上凭据只进 GitHub Secrets 和 Worker Secrets，绝不写进
  任何提交的文件。AI Key 在 D1 里加密存储，读出来一律脱敏，明文不进前端。
- 前端用手写 CSS，不用 Tailwind；理由和另外两处与需求文档不同的实现记在
  `docs/prd.md` §12.3。
- 三种宽度都要适配：手机 <640px、平板 640–1023px、PC ≥1024px；触控目标 44px；
  输入框字号 16px 防止 iOS 自动放大。
- 提交信息用中文，说清楚**为什么**这么改、踩到了什么，不要只写改了什么。
